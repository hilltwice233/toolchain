{
  "name": "toolchain-monorepo",
  "description": "Monorepo root for the toolchain repo.",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "rollup": "^4.3.0",
    "vitest": "^0.34.6"
  }
}
